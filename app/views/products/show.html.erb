<div class="container-fluid">
  <div class="d-flex mb-3">
    <div class= " col-lg-4">
      <div id="carouselExampleControls" class="carousel slide" data-ride="carousel">
      <div class="carousel-inner">
        <%  @product.photos.each do |photo|  %>
          <div class="carousel-item <%= photo == @product.photos.first && "active" %>">
            <%= cl_image_tag photo.key, class: "d-block w-100" %>
          </div>
        <% end %>
    </div>
    <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
      <span class="carousel-control-prev-icon" aria-hidden="true"></span>
      <span class="sr-only">Previous</span>
    </a>
    <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
      <span class="carousel-control-next-icon" aria-hidden="true"></span>
      <span class="sr-only">Next</span>
    </a>
    </div>
  </div>
    <div class="card-body-white col-6">
      <p> </p>
        <h3> <%= @product.designer.name %> </h3>
        <p></p>
        <h2> <%= @product.name %> </h2>
        <p></p>
        <p class="card-text"> <%= @product.description %> </p>
        <h2> <%= @product.price %> EUR </h2>
        <div class="form-group col-6 pl-0">
      <label for="inputState"></label>
      <select id="inputState" class="form-control">
        <option selected>Size</option>
        <option>S</option>
        <option>M</option>
        <option>L</option>
      </select>
        <div>
          <%= link_to product_orders_products_path(@product), method: :post do%>
           <button class="btn btn-light w-100" id="add-to-cart-button" type="submit">Add to my cart</button>
           <% end %>
           <p></p>
           <p>Ships from Germany | FREE Shipping & Returns within Germany</p>
         </div>
       </div>
    </div>
</div>

  <div class="other-products">
   <p> You Might Also Like </p>
  </div>
  <div class= "row">
  <% @random_products.each do |product| %>
  <div class="col-lg-3 col-md-6 col-sm-12">
      <div class="card-other-products " style="width: 18rem;">
        <%= link_to product_path(product) do %>
         <%= cl_image_tag product.photos.first.key, class: "card-img-top card-image-size" %>
          <div class="card-body-white">
            <div class="card-text">
              <h3> <%= product.designer.name %> </h3>
              <h2 class="name"> <%= product.name %> </h2>
              <h2 class="price"> <%= product.price %> EUR </h2>
            </div>
          </div>
         <% end %>
        </div>
      </div>
    <% end %>
  </div>





