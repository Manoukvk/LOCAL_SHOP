<div class="col-lg-4 " >
 <%= link_to product_path(product) do  %>
     <div class=" d-flex flex-column  justify-content-between card card-center p-3" style="width: 22rem; height: 40rem; margin-bottom: 4rem;" >

 <div class=" h-100">
      <div class="" style=" height: 80px;">
        <h3> <%= product.designer.name %> </h3>
        <h2> <%= product.name %> </h2>
        <!-- <p class="card-text"> <%= product.description %> </p> -->
      </div>

      <%= cl_image_tag product.photos.first.key, class: "card-img-top  w-100" %>
 </div>
  <% end %>
      <div class=" d-flex justify-content-between align-items-center pl-3 pr-3 pb-2 pt-2">
        <p class="mb-0"> <%= product.price %>,- € </p>
        <p class="mb-0"><%= "Available size" %> </p>
        <div>
        <%= link_to product_orders_products_path(product), method: :post do%>
        <i class="fas fa-shopping-cart"></i>
        <% end %>
        </div>
      </div>
    </div>


  </div>

